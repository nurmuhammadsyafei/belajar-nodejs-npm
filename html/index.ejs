<!DOCTYPE html>
<html>
  <head>
    <title>Users</title>
  </head>
  <body>
    <h1>Users</h1>
    <ul>
      <% users.forEach(user => { %>
        <li><%= user.first_name %> - <%= user.first_name %></li>
      <% }); %>
    </ul>
  </body>
</html>
